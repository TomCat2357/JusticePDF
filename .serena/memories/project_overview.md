# PDFas overview
- Visual PDF merge/split desktop app with drag & drop workflows.
- Entry point: `python -m src.main` (PyQt6 GUI).
- Core features: PDF card reordering, merge via drop, import external files, page reordering, page extraction.
