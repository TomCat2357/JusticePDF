# Code style & conventions
- No explicit formatter/linter config found (only `pyproject.toml` with deps).
- Code uses module and function docstrings in many files.
- Naming: snake_case for functions/vars; classes use PascalCase.
- Type hints: limited/none in sampled entry point; follow existing file patterns.
