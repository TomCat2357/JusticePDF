# Suggested commands
- Install (uv): `uv sync`
- Install (pip): `pip install -e .`
- Run app: `python -m src.main`
- Debug log (Windows): `set PDFAS_DEBUG=1` then `python -m src.main`
- Tests: `pytest`
